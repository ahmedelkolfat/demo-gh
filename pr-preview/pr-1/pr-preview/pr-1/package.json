{
  "name": "demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean:allure": "rimraf allure-report/ && rimraf allure-results",
    "run:test": "npm run clean:allure && playwright test",
    "generate:allure": "allure generate allure-results -o allure-report --clean && allure open"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.35.1",
    "allure-playwright": "2.2.1",
    "rimraf": "5.0.0"
  }
}
